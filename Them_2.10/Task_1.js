if ("0") {
    alert( 'Привет' );
  }
  // Да, на экран выведется Привет, потому, что "0" - true.
  // Если бы в скобках после оператора if был бы 0 - значение приняло бы false и на экране бы ничего не отобразилось.